
int baz(int x)
{
  return x++ + x++;
}

int foo(int x, int y, int z)
{
  if (x && (y || z))
    return 0;
  return 1;
}

int bar(int x, int y)
{
  int z = x && y;
  if (z)
    return 0;
  return 1;
}

