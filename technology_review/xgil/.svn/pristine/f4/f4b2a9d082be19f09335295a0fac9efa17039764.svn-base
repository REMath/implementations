
// test coercions and modelling of expression sign/width.

/*
void foo(int x)
{
  int y = x * 4;
  int z = x * y;
}

short bar(unsigned int taglen)
{
  if (taglen >= ~(unsigned int)0 / 2)
    return 1;
  return (short) taglen;
}
*/

int baz(int x, unsigned int y, unsigned int z)
{
  int newx = x & -3;
  unsigned int newy = y & -3;
  unsigned int newz = z & 4;
  unsigned int addz = z + 4;
  short wtf = x + y;
}
